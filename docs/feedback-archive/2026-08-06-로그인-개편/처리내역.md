# 2026-08-06 — 로그인 창 개편 피드백 (PPW 5.1 로그인 참고)

**피드백** (스크린샷 4장 — PPW 5.1 로그인·DB Config 화면, gitignore 대상 로컬 보관):
1. 좌측 홍보 이미지 패널은 불필요
2. 로그인에서 서버(DB Config)를 바꿀 수 있게
3. CHANGE PW 버튼 제거

**처리** (v0.1.11):
- 로그인 창 단일 컬럼 재구성: 로고+버전(Ver x.y.z) / USER ID / PASSWORD / **서버 콤보** /
  LOG IN / 오프라인으로 계속 · 종료(EXIT) / 저작자 표기
- 서버 콤보 마지막 항목 **"서버 설정… (DB Config)"** → 관리 모달(ServerConfigWindow):
  이름+URL 목록 추가/수정/삭제, [확인]에서만 저장. `%AppData%/sh-dicom-studio/servers.json`,
  마지막 선택 서버 기억
- CHANGE PW 없음 (비밀번호 변경은 추후 서버 계정 관리에서)
