# 2026-08-06 — UI 전면 개편 피드백

**피드백** (스크린샷 `스크린샷 2026-08-06 102322.png` — gitignore 대상, 로컬 보관):
좌측이 sh-dicom-studio v0.1.0, 우측이 기존 Electron 판 DICOM Studio 비교.

1. 전체적으로 조잡하고 어두워서 글씨가 안 보인다
2. 레이아웃을 바둑판(N×N 픽커)으로 고르고 싶다
3. 제작자 라이센스가 UI 에 노출되지 않는다
4. 도구들이 묶음(그룹)으로 안 보인다

**처리** (v0.1.1):
- 라이트 크롬 + 다크 뷰어 테마로 전환, Electron 판과 동일한 팔레트(#3D6BF5 액센트)
- 사이드바를 MAIN TOOLS / EXAM INFORMATION / IMAGE TOOLS 섹션으로 재구성 —
  VPWinGate 매뉴얼의 버튼 구성을 전부 배치(미구현은 비활성+예정 툴팁)
- 바둑판 레이아웃 픽커(`LayoutPicker`, 6×6 호버 하이라이트) 추가
- 하단에 저장 버튼(M3 예정) + 제작·저작권 표기 추가
- 헤드리스 스크린샷 도구(`tools/ShotTool`) 도입 — WSL 에서 UI 시각 확인 가능
